import { defineCloudflareConfig } from "@opennextjs/cloudflare";

export default defineCloudflareConfig({
  // ตั้งค่า default configuration สำหรับ Cloudflare
  // สามารถปรับแต่ง caching และ features อื่นๆ ได้ที่นี่
  // ดูรายละเอียดเพิ่มเติมได้ที่: https://opennext.js.org/cloudflare/caching
});
